// import { take_Student} from "./studentTypes";

// const initialState = {
//   numOffStudents: 10,
// };

// const reducer = (state = initialState, action) => {
//   switch (action.type) {
//     case take_Student:
//       return {
//         ...state,
//         numOffStudents: state.numOffStudents > 0 ? state.numOffStudents - 1 : 0,
//       };
//     default:
//       return state;
//   }
// };

// export default reducer;
